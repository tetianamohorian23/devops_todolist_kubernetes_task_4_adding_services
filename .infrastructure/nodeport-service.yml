apiVersion: v1
kind: Service
metadata:
  name: todoapp-nodeport
  namespace: todoapp
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080